# Shell scripts must keep LF endings — they run on Linux on the VM.
# Without this, autocrlf on Windows checkins flips them to CRLF and
# `bash: /bin/bash^M: bad interpreter` breaks the VM bench loop.
*.sh text eol=lf
