# vgc-ai TASKS

> **Work queue for the autonomous Claude loop running on the GCP VM.**
> The loop reads this file, picks the top **Approved** item with status `todo`, and works only on that.
> The loop NEVER invents tasks. If it has an idea, it appends to **Proposed for human review**.
>
> **You** (the human) maintain the Approved list. **Claude** updates statuses (`todo` → `wip` → `done` / `blocked`).
> Format: `- [STATUS] <slug>: <description>`.

## Approved

<!-- Pre-approved tasks Claude can pick from. Keep ordered by priority — Claude takes the top todo. -->

- [done] chore-gitignore-models: Add `models/` to `.gitignore`. The `policy-tabular-mc-train` blocked branch referenced this entry but never landed on `main`, so any subsequent loop iteration that runs `scripts/train_tabular_mc.py` leaves a multi-MB `models/tabular_mc.json` as untracked working-tree state, which `git status` flags and is at risk of accidental staging on PR creation. Per CLAUDE.md "Don't commit large simulation artifacts" + the gitignore convention already used for `bench/leaderboard.csv` / `bench/elo.csv` / `bench/results/`. Acceptance: `models/` appears in `.gitignore`; `git status` on a checkout that has run training is clean. No bench evidence required (housekeeping). **Why:** uncovered while running `policy-heuristic-eval-deterministic-rollout-n2000` — the n=2000 task accidentally staged `models/tabular_mc.json` (left behind by an earlier task on the same VM) via `git add -A`; had to unstage manually. Same pattern caught the original `bench-elo-gitignore` task. Cost: 1-line .gitignore change. **Verified:** added `models/` line to `.gitignore` alongside the existing `bench/leaderboard.csv` / `bench/elo.csv` / `bench/results/` entries; `git status` on the VM (which had `models/tabular_mc.json` as a ~20MB untracked artifact left behind by the prior MC training tasks) is now clean — the `models/` directory no longer appears in `git status` output. ruff format / ruff check / mypy --strict / pytest (33 tests) all green. PR #10.

- [todo] chore-baseline-promote-heuristic-det: Now that `heuristic_det` has certified at the gate (win_rate=0.5405, ci95_low=0.5186 over Greedy at n=2000), update the bench harness so the default `--b` baseline is `heuristic_det` rather than `greedy`. This raises the bar: future policy attempts must beat `heuristic_det`, not just Greedy. Also update `policy-greedy-switch-tighten-*` blocked entries to note they're now measured against a stronger baseline. Acceptance: `bench/run_once.py` and `bench/run_continuous.py` accept the new default; `bench/leaderboard.csv` continues to record per-matchup rows; existing tests still pass. **Why:** the current Approved queue's MC-training tasks (`policy-tabular-mc-train-with-loss-signal`, `policy-tabular-mc-train-vs-greedy-warmup`) still gate on "beat greedy" — but now that we have a better baseline, the project's submission policy should be the new strongest known. Open sub-decision: keep `greedy` as an *additional* matchup (informative) vs. drop it (cheaper). Recommend keeping it for now.

- [todo] policy-tabular-mc-canonicalize-action-idx: Replace `TabularMCBattlePolicy`'s positional `action_idx` (currently the index into `get_actions(team_pair)`'s cartesian product) with a **canonical action key** that is stable across turns regardless of move-PP / disabled / reserve-alive changes. Concrete design: encode each per-slot `BattleCommand` as `(kind, payload)` where `kind ∈ {MOVE, SWITCH}` and `payload` is `move.constants.id` (or, if `id` is `-1`/unset across all generated moves, a derived stable signature `(pkm_type.value, base_power, int(accuracy*100), category.value)`) for moves, and `reserve.constants.id` (same fallback) for switches; the joint action key is the tuple of per-slot canonical commands. Change `self._q` / `self._n` from `dict[StateKey, list[float]]` to `dict[StateKey, dict[ActionKey, float]]` (and `int` for n). Update `decision()` to iterate the legal joint actions, canonicalize each, and argmax over the dict (falling back to random when no canonicalized key has been visited). Re-train via `scripts/train_tabular_mc.py` for 10000 episodes self-play, with the **winner-only** terminal reward (the simplest baseline before re-introducing the loss signal — to isolate this single change). Acceptance: standard gate at n=200 vs `greedy` AND the Q-table key count is meaningfully lower than the 43844-key positional-baseline run (signaling state-action collapse from canonicalization is happening). **Why:** unanimous next-move from the three blocked MC entries (`policy-tabular-mc-train`, `…-with-loss-signal`, `…-vs-greedy-warmup`). The cumulative MC evidence (winner-only 0.055 → loss-signal 0.035 → warmup+loss 0.075) shows the ceiling is bounded by signal conflation, not training regime: a positional `action_idx` denotes different commands across visits to the same `state_key` (since `battling_moves` ordering and `reserve` length both drift turn-to-turn), so first-visit MC averages incomparable returns into the same cell. Each blocked entry explicitly says "do not retry as a follow-up without first addressing (a) action indexing." This task is (a). Encoder refinement (the consistent diagnosis #2) is intentionally NOT bundled — separate proposal will follow if this lands and still doesn't clear the gate.

- [todo] policy-heuristic-eval-det-2ply: Deepen the `heuristic_det` 1-ply rollout to 2-ply (opponent-best-response then re-evaluate). Determinism (ZERO_RNG accuracy, ONE_RNG eff/sta) makes the deeper rollout cheap and reproducible. Current `avg_turn_ms_a=25.6ms` at 1-ply leaves ~975ms headroom under the sub-second-per-turn budget — a 2-ply expansion of ~K² instead of K should still fit comfortably. Acceptance: standard gate at n=200, `avg_turn_ms_a < 500ms`. **Why:** if `policy-heuristic-eval-deterministic-rollout-n2000` (Approved) certifies the heuristic family at the gate, this is the natural follow-up to push the edge further; if it does not, this is a meaningfully different policy (depth, not n) worth one more attempt before fully pivoting to MC. Open sub-decision: opponent model in the 2-ply rollout — assume opponent is greedy (realistic, matches bench baseline) or assume opponent uses the same heuristic (paranoid, better self-play hardening). Recommend greedy-opponent for first attempt.

- [done] policy-heuristic-eval-deterministic-rollout-n2000: Re-run the existing `heuristic_det` policy (already on `main`) at n=2000 vs `greedy` to certify whether the 0.52-0.53 true-win-rate estimate from n=500 (win_rate=0.528, ci95_low=0.4842) clears the gate. At n=2000 with point estimate 0.528, Wilson `ci95_low ≈ 0.506` — just barely clears. Cost: ~8 min wall (avg_battle_ms=241.93 × 2000), within iteration budget. Run `bench/run_once --a heuristic_det --b greedy --n 2000 --output bench/results/policy-heuristic-eval-deterministic-rollout-n2000.json`. Acceptance: `win_rate_a > 0.5` AND `ci95_low > 0.5` AND `avg_battle_ms < 5000`. **Why this comes first:** cheapest path to *any* gate-clearing heuristic-family policy. If it fails, that family is conclusively dead at this state-eval design and all remaining policy effort pivots to MC training (next two tasks). If it passes, we have the first real candidate to replace `GreedyBattlePolicy` as the submission default. Why: cherry-picked aad02fc onto branch `auto/policy-heuristic-eval-deterministic-rollout-n2000-20260512` (the `heuristic_det` policy was NOT on `main` despite the task wording — it lived only on the prior blocked branches, same situation the n=500 retry hit); ruff/mypy --strict/pytest all green (36 tests). 2000-game bench `heuristic_det` vs `greedy`: **win_rate_a=0.5405 (wins_a=1081/2000), ci95_low=0.5186 ✓, ci95_high=0.5622, avg_battle_ms=236.19, avg_turn_ms_a=24.7ms / avg_turn_ms_b=1.193ms, elapsed_sec=472.38** — **first policy in the project to clear the gate.** Point estimate (0.5405) landed slightly higher than the n=500 estimate (0.528) but well within the n=500 CI [0.4842, 0.5714]; the +4.05% true edge over Greedy is now statistically distinguishable from a coin flip at 95% confidence. avg_battle_ms is 21× greedy-vs-greedy but the per-turn cost (24.7ms) leaves ~975ms headroom under the sub-second-per-turn budget, so the policy is submission-viable on latency. **Implication:** `heuristic_det` is the first real candidate to replace `GreedyBattlePolicy` as the submission default. Recommended follow-ups (see Proposed): (a) make `heuristic_det` the new bench baseline so the gate now means "beat heuristic_det" rather than "beat greedy"; (b) promote `policy-heuristic-eval-det-2ply` (already in Proposed) to push the edge further now that the family is validated; (c) re-evaluate the MC training tasks against the new baseline. PR #9.

- [blocked] policy-tabular-mc-train-with-loss-signal: Modify `scripts/train_tabular_mc.py` to also record **losing-side** trajectories with terminal reward `g=-1` (currently winner-only with `g=+1`; losing trajectories are discarded). Re-train 10000 self-play episodes, persist to `models/tabular_mc.json`, bench `tabular_mc` vs `greedy` at n=200. Acceptance: `win_rate_a > 0.5` AND `ci95_low > 0.5` AND `avg_battle_ms < 5000`. **Why:** the `policy-tabular-mc-train` blocked entry's diagnosis #1 (highest leverage): without a negative signal, Q-values monotonically ratchet up and first-visit MC can't distinguish "this action led to a win" from "this action got me KO'd but the team carried." This is the smallest principled fix that targets the root-cause signal architecture; keeps every other piece of the pipeline (state encoder, action indexing, ε-schedule, episode count) constant so the result isolates the variable. If this clears the gate, MC training is a viable lever and we scale to 100k/1M episodes next. If it doesn't, next move is the state-encoder / action-indexing fixes (diagnoses #2 and #4 in the blocked entry). **Out of scope:** combining with the warm-up fix (that's the next task) — do them sequentially so we can attribute which fix moved the needle. Why: implemented on branch `auto/policy-tabular-mc-train-with-loss-signal-20260512` (cherry-pick of 9fe4a03 to bring the training script back onto a fresh branch — it was never on `main` per the prior blocked task; then surgical change isolated to `_run_training_episode` and the `train` loop). Ruff/mypy --strict/pytest all green (33 tests). `scripts/train_tabular_mc.py` ran 10000 self-play episodes (both trajectories now retained per episode: winner's last step gets +1.0, loser's last step gets -1.0; ties still discarded) in 101.09s at ~98.92 eps/sec on the VM, producing an 86805-key Q-table (~2× the 43844 keys from the winner-only run, as expected since both sides' state visits now contribute keys instead of just the winner's; self-play wins balanced: wins_0=5010 wins_1=4954 ties=36). **Bench fails the gate, and *regressed* from the winner-only baseline**: 200-game `tabular_mc` vs `greedy`: **win_rate_a=0.035 (wins_a=7/200), ci95_low=0.0171, ci95_high=0.0705** — worse than the winner-only run (0.055/0.031), worse than `random` vs `greedy` (~0.042 implied by the leaderboard's greedy-vs-random win rate). avg_battle_ms=3382.43 (under the 5000ms budget but ~2× the winner-only 1742ms — the policy makes even more non-progressing choices that drag battles out before losing). avg_turn_ms_a=0.071ms (lookup is cheap). **Hypothesis "loss signal is the smallest principled fix" is falsified.** Diagnosis: in self-play with a symmetric encoder, the winning and losing trajectories share many state-action visits across episodes (the same `(state_key, action_idx)` appears in episodes where it's the winner AND episodes where it's the loser, because the encoder is lossy and action_idx is positional). First-visit MC averages incremental returns: when those returns are `+1` on some visits and `-1` on others, Q converges toward ~0 with noise breaking ties. Selecting argmax over near-zero Q values is essentially picking by noise → strictly worse than the winner-only run, which at least had a monotonic Q signal even if the signal was uncalibrated. The loss signal *exposes* the encoder/action-index lossiness rather than fixing the signal architecture. Unblock paths, in order of leverage now that signal-only is falsified: (a) **stabilize action indexing first** (diagnosis #4 in the original `policy-tabular-mc-train` entry) — canonicalize action_idx as `(active_slot, command_kind, target_id)` instead of positional, so `+1` and `-1` updates don't conflate; only *then* re-enable the loss signal. (b) **refine the state encoder** to include opponent active typing and per-active move availability (diagnosis #2) so winning- and losing-side state visits actually diverge before averaging. (c) Reconsider the `policy-tabular-mc-train-vs-greedy-warmup` task (next in queue) — if the warm-up is run with the loss signal *active*, it will hit the same averaging problem (greedy plays nearly deterministically, so the same state-action pair will accumulate both +1 and -1 across episodes); recommend running the warm-up task with the loss signal *off* first to isolate the warm-up effect, then layer signal on top once action indexing is stabilized. **Do not retry as a follow-up without first addressing (a).**

- [blocked] policy-tabular-mc-train-vs-greedy-warmup: Building on `policy-tabular-mc-train-with-loss-signal` (do that one first; this task assumes the loss-signal fix is already in `scripts/train_tabular_mc.py`), additionally modify the training loop to use single-sided rollouts vs `greedy` (one `TabularMCBattlePolicy`, one `GreedyBattlePolicy` opponent) for the first 5000 episodes, then switch to self-play for episodes 5000-10000. Alternate which side TabularMC plays each episode for balanced state coverage. Acceptance: same gate as the loss-signal task. **Why:** diagnosis #3 in the original blocked entry — random-vs-random self-play produces near-random Q-bumps for thousands of trajectories before any signal compounds. Warm-up vs a known-non-trivial opponent bootstraps the table with realistic state visits and credible signal; self-play then takes over for the harder regime greedy can't drive. Compute-budget compensator for the fact that we have 113 eps/sec, not the 2024 winner's ~30M-trial budget. Why: implemented on branch `auto/policy-tabular-mc-train-vs-greedy-warmup-20260512` (cherry-picked base training script and loss-signal commits from the prior `auto/policy-tabular-mc-train-with-loss-signal-20260512` branch since neither landed on `main`; the warmup phase + `--warmup` CLI flag is the only new logic). Ruff/mypy --strict/pytest all green (33 tests). Training ran 10000 episodes in 140.5s (eps_per_sec=71.18; warmup runs at ~55 eps/sec because each turn now invokes Greedy on one side, self-play recovers to ~72): warmup phase produced `warmup_tab/opp=185/4810` (tabular won only 185/5000 = 3.7% vs Greedy during warmup, as expected for an untrained policy with positional action indexing); self-play phase finished with balanced wins (wins_0=5010, wins_1=4969, ties=21) and grew the Q-table to 57289 keys (mid-way between the 43844 winner-only and 86805 loss-signal-only baselines). **Bench fails the gate**: 200-game `tabular_mc` vs `greedy`: **win_rate_a=0.075 (wins_a=15/200), ci95_low=0.046, ci95_high=0.12** — well below the 0.5 gate (off by 0.454). avg_battle_ms=2214.4 (under the 5000ms budget); avg_turn_ms_a=0.071ms (lookup is cheap). Comparison to siblings: winner-only self-play (0.055/0.031), loss-signal self-play (0.035/0.0171), warmup + loss-signal (0.075/0.046). Warmup mitigated the +1/-1 averaging problem somewhat (0.075 > 0.035) and recovered most of the winner-only baseline (0.075 vs 0.055), but did not unblock the gate. Diagnosis: the blocked entry on `policy-tabular-mc-train-with-loss-signal` predicted exactly this outcome: "if the warm-up is run with the loss signal active, it will hit the same averaging problem (greedy plays nearly deterministically, so the same state-action pair will accumulate both +1 and -1 across episodes)" — confirmed. During warmup, the tabular policy lost 96.3% of episodes, so almost every trajectory carries g=-1 terminal reward; with positional `action_idx` and the 11-dim collapsed state encoder, the Q-table is dominated by -1 averages on the same (state_key, action_idx) pairs that occasionally got +1 from the 3.7% wins, leaving argmax essentially driven by tiebreak noise. The fundamental signal-conflation problem identified in the original `policy-tabular-mc-train` blocked entry (diagnosis #4: positional action_idx) is NOT fixed by warm-up vs greedy alone. Unblock paths, in order of expected leverage now that warm-up alone is falsified: (a) **stabilize action indexing** (the prior task's diagnosis #4) — canonicalize action_idx as `(active_slot, command_kind, target_id)` instead of positional, so +1/-1 updates don't conflate across distinct decisions; this is the single highest-leverage next move per the cumulative MC evidence (winner-only 0.055 → loss-signal 0.035 → warmup+loss 0.075 — the level is bounded by the conflation, not the training regime). (b) **refine the state encoder** to include opponent active typing, per-active move availability, and move-PP — without this, even after fixing action indexing, the encoder cannot distinguish many positions that have different correct decisions. (c) **try warm-up with the loss signal disabled** (the original blocked entry's suggestion) to see if the warm-up alone recovers above winner-only baseline — this is a clean ablation rather than a leverage move, but it would isolate the warm-up effect from the signal conflation. **Do not retry as a follow-up without first addressing (a).**

- [blocked] policy-heuristic-eval-deterministic-rollout-n500: Re-run the existing `heuristic_det` policy at n=500 vs `greedy` to test whether its 0.560 point estimate at n=200 (ci95_low=0.4907, the best result of any policy attempt to date) is real signal. Use the policy already on `main` (do not re-implement) and run `bench/run_once --a heuristic_det --b greedy --n 500 --output bench/results/policy-heuristic-eval-deterministic-rollout-n500.json`. Acceptance: `win_rate_a > 0.5` AND `ci95_low > 0.5` AND `avg_battle_ms < 5000`. **Why this comes first:** the loop's analysis flagged this as the cheapest, highest-probability path to a real policy win. ~2 min wall-clock. If it fails, the heuristic-eval family is genuinely capped near +5% and the next move is MC training (below). No code changes expected — just bench + verify. Why: implemented on branch `auto/policy-heuristic-eval-deterministic-rollout-n500-20260512` (cherry-pick of aad02fc — the `heuristic_det` policy was NOT on `main` despite the task wording; it lived only on the prior blocked branch); ruff/mypy --strict/pytest all green; 500-game bench `heuristic_det` vs `greedy`: win_rate_a=0.528, **ci95_low=0.4842** (< 0.5 gate, *further* from 0.5 than the n=200 result), ci95_high=0.5714, avg_battle_ms=241.93 (well under budget), avg_turn_ms_a=24.6ms / avg_turn_ms_b=1.19ms. The hypothesis "if true edge is ~0.56, n=500 will certify the gate" is falsified: the point estimate regressed from 0.560 (n=200, wins_a=112/200) to 0.528 (n=500, wins_a=264/500), consistent with the n=200 result being high-variance noise above a true win rate closer to ~0.52-0.53 (in line with the rest of the heuristic family: greedy_switch 0.535, heuristic 0.540, heuristic-det now 0.528 at n=500). **Implication per task wording:** "the heuristic-eval family is genuinely capped near +5% and the next move is MC training" — i.e. `policy-tabular-mc-train` (the next `[todo]` item) is now the planned lever. Unblock paths for this specific task: (a) re-run at n=2000+ to certify the ~0.52-0.53 estimate (would need ~8min wall, well within budget; ci95_low for win_rate=0.528 at n=2000 is ~0.506, just barely clearing the gate — but the true rate at n=∞ may be even lower); (b) accept the heuristic family caps near coin-flip and pivot fully to MC training; (c) try the proposed 2-ply deterministic variant (`policy-heuristic-eval-det-2ply` in Proposed) for a meaningfully different policy rather than a bigger sample.

- [blocked] policy-tabular-mc-train: Train the existing `TabularMCBattlePolicy` skeleton (already in `src/vgc_ai/policies/tabular_mc.py` from PR #7) via **self-play** and bench the trained checkpoint vs `greedy`. Concrete plan:
    1. Add `scripts/train_tabular_mc.py` that runs N self-play episodes (start N=10000). Each episode: instantiate two `TabularMCBattlePolicy` agents sharing one Q-table, play one battle via `vgc_ai.eval.duel.duel` (or equivalent), record the trajectory of `(encoded_state, action, reward)` tuples for the winning side, call `policy.learn(trajectories)` after each batch of 100 episodes.
    2. Action selection during training: **ε-greedy** with ε=0.1 for the first 5000 episodes, decaying linearly to ε=0.05 by episode 10000. Random tiebreak when all values are equal.
    3. **Unbounded** Q-table for now — if memory grows unreasonably (>500MB resident), mark `blocked` with the observed size; we add LRU eviction in a follow-up.
    4. Save the trained Q-table to `models/tabular_mc.json`. **Sub-task: add `models/` to `.gitignore`** as part of this commit (CLAUDE.md says MC visit tables must be gitignored).
    5. Modify `TabularMCBattlePolicy` (or add a CLI flag) to load `models/tabular_mc.json` on instantiation if present; bench picks up the trained version automatically.
    6. Run `bench/run_once --a tabular_mc --b greedy --n 200 --output bench/results/policy-tabular-mc-train.json` after training completes.

    Acceptance: trained checkpoint exists at `models/tabular_mc.json` and is non-empty; `models/` is in `.gitignore`; `bench.run_once` shows `win_rate_a > 0.5` AND `ci95_low > 0.5` AND `avg_battle_ms < 5000`. **Iteration budget:** if 10000 episodes don't fit in the 1-hour iteration cap, commit progress, save what you have to `models/tabular_mc.json`, mark `[wip]` with a concrete `Why:` (episodes/sec achieved, est. time for 10k). The loop's next iteration can resume training from the existing checkpoint. **Note on training data:** if the trained policy beats greedy meaningfully, scaling N to 100k or 1M episodes in follow-up tasks is on the table — that's the AurelianTactics 2024 approach (~30M trials). Don't ship a partially-trained model as the new submission default; just produce evidence the approach works. Why: implemented on branch `auto/policy-tabular-mc-train-20260512`; ruff/mypy --strict/pytest all green (33 tests). `scripts/train_tabular_mc.py` ran 10000 self-play episodes (TabularMC vs TabularMC, ε-greedy 0.1→0.05 linear over 10k, winner-only terminal reward +1.0, losing trajectories discarded as the task specifies) in 88.04s at ~113.58 eps/sec on the VM, producing a 43844-key Q-table (16MB JSON at `models/tabular_mc.json`, gitignored via new `models/` entry); self-play wins were balanced (wins_0=4887 wins_1=5063 ties=50, indicating no broken self-play asymmetry). `TabularMCBattlePolicy.__init__` now takes an optional `model_path` kwarg (default `None`, no auto-load — tests untouched); registry entry `tabular_mc` is wrapped via `partial(TabularMCBattlePolicy, model_path="models/tabular_mc.json")` so bench picks up the checkpoint automatically. **Bench fails the gate**: 200-game `tabular_mc` vs `greedy`: **win_rate_a=0.055**, ci95_low=0.031, ci95_high=0.0958 — far below the 0.5 gate (off by 0.469), and roughly identical to what `random` would score vs `greedy` (~0.04 implied by the leaderboard's 95.80% greedy-vs-random win rate). avg_battle_ms=1742.36 (under the 5000ms budget but ~80× the greedy-vs-random baseline of ~20ms — strongly suggests the trained policy makes many non-progressing choices, dragging out battles before losing). avg_turn_ms_a=0.072ms (the policy lookup itself is cheap). Diagnosis: (1) **winner-only reward signal is too weak** — Q-values monotonically ratchet up across episodes, so the policy can't distinguish "this action led to a win" from "this action got me KO'd but the team carried"; without a negative signal for losses, first-visit MC does not converge to a useful policy on this signal architecture. (2) **state encoding too coarse / action-index unstable** — 11 dims (HP-quartile buckets + status + alive-count + weather) collapses many distinct decision states; `get_actions` list length/ordering varies turn-to-turn (PP/disabled/reserve dynamics), so the same `action_idx` denotes different commands across visits to the same `state_key`, conflating updates across genuinely different decisions. (3) **self-play untrained-vs-untrained bootstrap** is essentially random-vs-random for the first thousands of trajectories — random Q-bumps to arbitrary state-actions before any signal compounds. Unblock paths, in order of expected leverage: (a) **also record losing trajectories with g=-1 (or 0)** — smallest principled change; let MC see the negative signal so Q-values can both go up and down. (b) **train vs `greedy` (single-sided) rather than self-play** for the first warm-up phase, then switch to self-play once Q is non-trivial. (c) **refine the state encoder** to include opponent active typing and per-active move availability (this is the larger lever per AurelianTactics 2024, which engineered an 11-dim space tailored to the engine, not just gross HP/status features). (d) **stabilize action indexing** (e.g. canonical move-id ordering instead of positional). (e) Scale episodes to 100k–1M (2024 winner used ~30M) — but per (a)/(b), the signal architecture is the bottleneck, not n; bigger n with this setup will not converge. **Do not retry as a follow-up without addressing (a) or (b) first.**

- [done] bench-elo-gitignore: Add `bench/elo.csv` to `.gitignore` (next to the existing `bench/leaderboard.csv` line). The file is a runtime artifact produced by `bench/run_continuous.py` and CLAUDE.md explicitly lists "ELO traces" alongside other simulation artifacts that must be gitignored. Acceptance: `git status` on a clean checkout is clean even after a `run_continuous.py` round; existing `bench/leaderboard.csv` ignore behavior unchanged. No bench evidence required (housekeeping). **Verified:** added `bench/elo.csv` line to .gitignore; `git status` now shows clean working tree (previously listed `bench/elo.csv` as untracked); ruff/mypy --strict/pytest all green. PR #8.

- [blocked] policy-greedy-switch-n500: Re-implement `GreedyWithSwitchBattlePolicy` on a fresh branch from current `main`, identical to the previous attempt on `auto/policy-greedy-switch-20260511` (HP<0.25 trigger; switch to a benched non-fainted Pokemon with a STAB super-effective move). Register as `greedy_switch`. Then run `bench/run_once --a greedy_switch --b greedy --n 500 --output bench/results/policy-greedy-switch-n500.json`. Acceptance: `win_rate_a > 0.5` AND `ci95_low > 0.5` AND `avg_battle_ms < 5000`. The prior n=200 attempt got win_rate=0.535, ci95_low=0.4659 — if the +3.5% edge is real, n=500 should push ci95_low above 0.5. Why: re-implemented on branch `auto/policy-greedy-switch-n500-20260512` (cherry-pick of d52ff6d); ruff/mypy --strict/pytest all green; 500-game bench `greedy_switch` vs `greedy`: win_rate_a=0.534, **ci95_low=0.4902** (< 0.5 gate), ci95_high=0.5773, avg_battle_ms=23.13 (well under budget). Same shape as n=200 (0.535/0.4659): the +3.4% edge is real-looking but the Wilson lower bound still has not cleared 0.5 at n=500. The hypothesis that "if the edge is real, n=500 will push ci95_low above 0.5" is falsified — true win rate is more likely ~0.53 than the hoped 0.55+, which would need ~n=2000 to clear the gate at 95% confidence. Unblock paths: (a) run at n=2000+ (cost: ~46s wall, well within budget — could be a follow-up task), or (b) accept that the +3% edge is too marginal and pivot effort to the tighten-a/b/c/d variants which target tighter switching rules.

- [blocked] policy-heuristic-eval-n500: Re-implement `HeuristicBattlePolicy` on a fresh branch from current `main`, identical to the prior attempt on `auto/policy-heuristic-eval-20260511` (heuristic state-eval over HP/faint/type matchup + 1-ply rollout). Register as `heuristic`. Then run `bench/run_once --a heuristic --b greedy --n 500 --output bench/results/policy-heuristic-eval-n500.json`. Acceptance: `win_rate_a > 0.5` AND `ci95_low > 0.5` AND `avg_battle_ms < 5000`. The prior n=200 attempt got win_rate=0.545, ci95_low=0.4758. Why: re-implemented on branch `auto/policy-heuristic-eval-n500-20260512` (cherry-pick of 83b72e9); ruff/mypy --strict/pytest all green; 500-game bench `heuristic` vs `greedy`: win_rate_a=0.540, **ci95_low=0.4962** (< 0.5 gate), ci95_high=0.5832, avg_battle_ms=197.42 (well under budget), avg_turn_ms_a=21.1ms / avg_turn_ms_b=1.24ms. Same shape as n=200 (0.545/0.4758) and as the parallel greedy-switch-n500 block (0.534/0.4902): the +4% edge is real-looking but Wilson lower bound still has not cleared 0.5 at n=500 — missed by 0.004. Hypothesis "n=500 will push ci95_low above 0.5" is falsified; true win rate likely ~0.54, which would need ~n=1200-1500 to clear the gate at 95% confidence. Unblock paths: (a) re-run at n=1500+ (cost: ~5min wall, within budget); or (b) pivot to the `policy-heuristic-eval-deterministic-rollout` variant which targets sim-noise reduction rather than larger n.

- [blocked] policy-greedy-switch-tighten-a: Implement `GreedyWithSwitchBattlePolicy` variant (a) — same as the base greedy-switch but additionally require the candidate bench Pokemon to be at **full HP** before switching in (rationale: avoid switching into a damaged mon that immediately faints). Register as `greedy_switch_a`. Acceptance: 200-game bench `greedy_switch_a` vs `greedy` shows `win_rate_a > 0.5` AND `ci95_low > 0.5` AND `avg_battle_ms < 5000`. Why: implemented on branch `auto/policy-greedy-switch-tighten-a-20260512`; ruff/mypy --strict/pytest all green (incl. new `tests/test_greedy_switch_a.py`); 200-game bench `greedy_switch_a` vs `greedy`: win_rate_a=0.525, **ci95_low=0.456** (< 0.5 gate), ci95_high=0.5931, avg_battle_ms=23.74 (well under budget), avg_turn_ms_a=1.123ms / avg_turn_ms_b=1.065ms. Same shape as the base `policy-greedy-switch` block (0.535/0.4659) — adding the full-HP gate did not visibly improve the win rate over the base variant at this sample size, and the +2.5% edge over Greedy is not statistically distinguishable from a coin flip at n=200. Unblock paths: (a) re-run at n=1500-2000 if a ~2% true edge is acceptable; (b) accept that the full-HP gate alone is too marginal and look at the remaining tighten variants (-b, -c, -d) before stacking constraints.

- [blocked] policy-greedy-switch-tighten-b: Implement variant (b) — same as base greedy-switch but **at most one switch per turn in doubles** even if both actives are below the HP threshold (rationale: tandem-switch trades momentum). Register as `greedy_switch_b`. Same acceptance gate as variant (a). Why: implemented on branch `auto/policy-greedy-switch-tighten-b-20260512`; ruff/mypy --strict/pytest all green (incl. new `tests/test_greedy_switch_b.py`); 200-game bench `greedy_switch_b` vs `greedy`: win_rate_a=0.540, **ci95_low=0.4708** (< 0.5 gate), ci95_high=0.6077, avg_battle_ms=25.66 (well under budget), avg_turn_ms_a=1.235ms / avg_turn_ms_b=1.182ms. Same shape as the base `policy-greedy-switch` block (0.535/0.4659) and tighten-a (0.525/0.456) — the at-most-one-switch-per-turn cap is on par with the base but does not visibly outperform it at n=200, and the +4% edge over Greedy is not statistically distinguishable from a coin flip. Unblock paths: (a) re-run at n=1500-2000 if a ~3-4% true edge is acceptable (cost: ~40-50s wall); (b) skip ahead to the remaining tighten variants (-c, -d) before stacking constraints; (c) try combining tighten-b with a smarter `_pick_switch` (e.g. variant -c's effectiveness-scored picker).

- [blocked] policy-greedy-switch-tighten-c: Implement variant (c) — score candidate bench Pokemon by `max(type_effectiveness × STAB_modifier)` over their moves and pick the best, instead of returning the first super-effective match (rationale: not all super-effective is equal). Register as `greedy_switch_c`. Same acceptance gate. Why: implemented on branch `auto/policy-greedy-switch-tighten-c-20260512`; ruff/mypy --strict/pytest all green (incl. new `tests/test_greedy_switch_c.py`); 200-game bench `greedy_switch_c` vs `greedy`: **win_rate_a=0.465** (regression, < 0.5), ci95_low=0.3972, ci95_high=0.5341, avg_battle_ms=24.0 (well under budget), avg_turn_ms_a=1.154ms / avg_turn_ms_b=1.182ms. The smarter picker actually performs *worse* than the base (0.535) and worse than first-match. Surprising — hypothesis: picking the single highest-score bench commits both slots in doubles to the same answer (since the picker is deterministic across slots, modulo the `used` set), which over-concentrates the response. Alternatively, ranking by `eff × stab` weights non-STAB 4x (score 4.0) above STAB 2x (score 3.0), but actual damage scales with base_power too — a 4x non-STAB low-base-power move may do less damage than a 2x STAB high-base-power move, so the picker can mis-rank. ci95 intervals [0.3972, 0.5341] for variant c overlap with [0.4659, 0.6028] for the base, so the apparent regression is not statistically distinguishable from noise at n=200, but the policy clearly fails to beat Greedy. Unblock paths: (a) include `base_power` in the score (e.g. `base_power * eff * stab`) so move-strength is weighted; (b) rank by score *only among STAB-super-effective moves* (preserving the original gate semantics more strictly); (c) try variant -d (slot-aligned answer) before further tuning -c; (d) re-run at n=1000+ to rule out noise before pivoting.

- [blocked] policy-greedy-switch-tighten-d: Implement variant (d) — require the candidate's super-effective opponent to be the **same opponent currently in front of the low-HP active** (slot-aligned), not "any opponent" (rationale: switching to threaten the wrong slot doesn't help the active). Register as `greedy_switch_d`. Same acceptance gate. Why: implemented on branch `auto/policy-greedy-switch-tighten-d-20260512`; ruff/mypy --strict/pytest all green (incl. new `tests/test_greedy_switch_d.py`); 200-game bench `greedy_switch_d` vs `greedy`: win_rate_a=0.535, **ci95_low=0.4659** (< 0.5 gate), ci95_high=0.6028, avg_battle_ms=23.88 (well under budget), avg_turn_ms_a=1.17ms / avg_turn_ms_b=1.108ms. Identical headline numbers to the base `policy-greedy-switch` block (0.535/0.4659) — the slot-aligned constraint is a strict subset of "any opponent", so on most boards the picker returns the same bench index (when the slot-aligned opp is also threatened by the candidate's STAB), and only differs on the minority of boards where the off-slot opponent is the only super-effective target. At n=200 that minority is too small to show up in the win rate. The tighten family (a/b/c/d) collectively shows the base trigger (HP < 25% + STAB super-effective answer) yields a ~3-4% true edge that the gate cannot certify at n=200. Unblock paths: (a) accept the ~3% edge is real but marginal and pivot effort to other policy families (heuristic-eval with deterministic rollout, tabular MC training) rather than continuing to tune the switch picker; (b) re-run any of the tighten variants at n=1500-2000 if a ~3% edge is acceptable as the operational baseline; (c) try a *combined* variant (e.g. tighten-b + tighten-d: at-most-one-switch-per-turn + slot-aligned) on the hypothesis that the constraints are complementary.

- [blocked] policy-heuristic-eval-deterministic-rollout: Re-implement `HeuristicBattlePolicy` on a fresh branch, identical to the prior attempt, **except** the 1-ply forward simulation inside `decision()` uses vgc2's deterministic-RNG path (`ZERO_RNG` or equivalent — find it via `grep -rn "ZERO_RNG\|deterministic" $(python -c "import vgc2; print(vgc2.__path__[0])")`). Rationale: weight-tuning at n=100 returned 50-50, so the bottleneck is more likely sim noise than weight calibration. Register as `heuristic_det`. Acceptance: 200-game bench `heuristic_det` vs `greedy` clears the standard gate. If `ZERO_RNG` doesn't exist or can't be used at the policy boundary, mark `blocked` with a concrete reason — do NOT monkey-patch vgc2. Why: implemented on branch `auto/policy-heuristic-eval-deterministic-rollout-20260512`; found `ZERO_RNG` and `ONE_RNG` in `vgc2/util/rng.py` (deterministic `Generator` subclasses already used by `TreeSearchBattlePolicy.get_states`); `forward()` accepts `acc_rng`/`eff_rng`/`sta_rng` kwargs, so no monkey-patching needed. New policy passes `acc_rng=ZERO_RNG` (moves always hit), `eff_rng=ONE_RNG` (secondary effects don't proc), `sta_rng=ONE_RNG` (status doesn't block) into the 1-ply rollout. Ruff/mypy --strict/pytest all green (incl. 3 new unit tests in `tests/test_heuristic_det.py`). 200-game bench `heuristic_det` vs `greedy`: win_rate_a=0.560, **ci95_low=0.4907** (< 0.5 gate), ci95_high=0.627, avg_battle_ms=236.65 (well under budget), avg_turn_ms_a=25.6ms / avg_turn_ms_b=1.24ms. The +6.0% edge is the largest of any policy attempted so far (greedy_switch 0.535, heuristic 0.545, this 0.560) — determinism does help, by ~1.5% over the stochastic-rollout heuristic — but the Wilson lower bound still misses 0.5 by 0.009 at n=200. If the true edge is genuinely ~0.56, n≈400-500 would clear the gate at 95% confidence. Unblock paths: (a) re-run at n=500-800 (cost: ~2-3min wall, well within iteration budget) — most likely to certify the existing edge; (b) stack determinism with a damage-prediction term in `evaluate` (current eval has no `base_power`-weighted damage forecast, only type effectiveness); (c) deepen lookahead to 2-ply now that determinism makes the rollout cheap and reproducible (current avg_turn_ms_a=25.6ms leaves ~970ms headroom under the sub-second/turn budget).

- [blocked] policy-registry: Extract `POLICIES` out of `src/vgc_ai/cli.py` into a new module `src/vgc_ai/policies/registry.py` so `bench/` and other consumers can import it without depending on the CLI module. Update `bench/run_once.py` and `bench/run_continuous.py` to import from the new location. Update `src/vgc_ai/cli.py` to re-export for backwards compatibility. Acceptance: `python -c "from vgc_ai.policies.registry import POLICIES; print(sorted(POLICIES))"` prints at least `['greedy', 'random', 'tree']`; `uv run pytest`, `uv run ruff check`, `uv run mypy --strict src` all pass; bench/run_once.py still works. No bench evidence required (this is a refactor, not a policy change). Why: implemented on branch `auto/policy-registry-20260511` but bench/import-acceptance pass — was blocked on issue #1 which is now fixed on main. **Human action needed:** either open a PR for that existing branch manually (it should rebase cleanly), or re-mark this `[todo]` so the loop redoes it on a fresh branch.

- [done] bench-fixed-teams: Add a `--fixed-team-seed N` flag to `bench/run_once.py` (and propagate through `bench/run_continuous.py` and `vgc_ai.eval.duel.duel`) so that, when set, the same `seed` is used to generate teams across all N battles in the round, making different policies compete on identical battles and dramatically reducing variance. Default behaviour (no flag) stays as today (fresh random teams per battle). Acceptance: `bench.run_once --a greedy --b random --n 5 --fixed-team-seed 42` returns the same `wins_a` count across 3 invocations; existing tests stay green; new test asserts determinism. No policy bench evidence required (infrastructure change). PR #3.

- [done] bench-elo: Maintain a running Elo rating per policy at `bench/elo.csv` (cols: `timestamp,policy,games,wins,losses,elo`). Each call to `run_continuous.py` updates Elo using the new matchup results (K=32, R0=1500). Acceptance: after 50 rounds of greedy-vs-random benching, `greedy`'s Elo is at least 200 above `random`'s. Add a unit test that simulates two rounds of fake results and asserts the Elo update formula matches a hand-computed value. **Verified:** 50 rounds × 20 battles → greedy 1711.17, random 1288.83, gap = 422.34. PR #4.

- [done] competitor-self-validate: Add `scripts/validate_competitor.py` that constructs two `VgcAiCompetitor()` instances and runs `vgc2.competition.match.Match` between them for 10 doubles battles (`n_active=2`), asserting that the match completes without error and reports a winner per battle. Used as a competition-submission smoke test. Acceptance: `uv run python scripts/validate_competitor.py` exits 0 in under 60 seconds; prints final win count. No bench evidence required. **Verified:** exits 0 in ~0.7s; 18 battles ran (Match tiebreaker pushed past the n=10 floor); printed `wins_a=8 wins_b=10`. PR #5.

- [done] bench-summary: Add `bench/summary.py` (module + CLI `python -m bench.summary --last N`) that reads `leaderboard.csv` and prints a Markdown table aggregating the last `N` rounds: rows are policies, columns are `vs greedy`, `vs random` (and any future baselines), cell is `win_rate (n=games)`. CLI default `--last 50`. Acceptance: script runs against the current leaderboard, prints valid Markdown, exits 0 on an empty leaderboard (prints "no data yet"). **Verified:** `uv run python -m bench.summary` against the current 52-row leaderboard prints a valid Markdown table (greedy 95.80% n=1000 vs random); `--path /tmp/nope.csv` prints `no data yet` and exits 0; 9 new unit tests cover aggregation/render/CLI edges.

- [blocked] policy-greedy-switch: Add a new battle policy `GreedyWithSwitchBattlePolicy` at `src/vgc_ai/policies/battle.py` that subclasses or composes `GreedyBattlePolicy` with one rule on top: if the active Pokemon's HP fraction is below 0.25 AND at least one benched Pokemon (a) is not fainted and (b) has a STAB move whose type is super-effective against the opponent's active, switch to that bench instead of attacking. Otherwise fall back to greedy. Register as `greedy_switch` in `POLICIES`. Acceptance: 200-game bench `greedy_switch vs greedy` shows `win_rate_a > 0.5` AND `ci95_low > 0.5` AND `avg_battle_ms < 5000`. If the heuristic isn't expressible via the vgc2 API exposed by `BattlePolicy`, mark `blocked` with a concrete reason — do NOT monkey-patch vgc2. Why: implemented on branch `auto/policy-greedy-switch-20260511`; ruff/mypy --strict/pytest all green (incl. new `tests/test_greedy_switch.py`); 200-game bench `greedy_switch` vs `greedy`: win_rate_a=0.535, **ci95_low=0.4659** (< 0.5 gate), ci95_high=0.6028, avg_battle_ms=23.48 (well under budget). Win rate is positive but the Wilson lower bound is below 0.5, so the +3.5% edge over Greedy is not statistically distinguishable from a coin flip at n=200 — same shape as the `policy-heuristic-eval` block. Unblock paths proposed below.

- [done] policy-tabular-mc-skeleton: Implement the skeleton of a tabular first-visit Monte Carlo battle policy at `src/vgc_ai/policies/tabular_mc.py`, following the structure of AurelianTactics 2024 (3rd place): collapsed integer state encoder (target ~11 dims), action-value table backed by a `dict[tuple, list[float]]`, `learn(trajectories)` method that does first-visit MC updates, JSON save/load. Register as `tabular_mc` in `POLICIES` (untrained instance acts uniformly at random when state unseen — this is by design). Acceptance: unit tests assert (a) the state encoder is deterministic across two encodings of the same `State`, (b) the encoded tuple has the same length across 20 random states; smoke-bench `tabular_mc vs random` runs end-to-end without crashing (no win required; the policy is untrained, table is empty); `avg_battle_ms < 5000`. **Out of scope:** training the policy. That's a follow-up task once this skeleton lands. **Verified:** 11-dim encoder; 7 unit tests cover encoder determinism, length consistency across 20 random states, untrained legality, save/load round-trip, first-visit MC update math; smoke bench `tabular_mc` vs `random` n=200: 96-104-0 (untrained ≈ random, as expected), avg_battle_ms=10.19, avg_turn_ms_a=0.056ms. PR #7.



- [done] bench-turn-timing: Add `avg_turn_ms_a` and `avg_turn_ms_b` columns to the bench output (both `run_once.py` JSON and `run_continuous.py` CSV). To get per-turn timing, instrument `vgc_ai/eval/duel.py` to wrap each policy's `get_action` call (or equivalent) with `time.perf_counter()` and accumulate. If vgc2's `BattlePolicy` interface doesn't expose a hookable method, mark `blocked` with a concrete reason — do not monkey-patch vgc2 internals. Acceptance: leaderboard.csv has the two new columns populated with non-zero values; existing rows are preserved (the writer should handle the schema migration via header-rewrite on first new round). No policy bench evidence required. PR #2.

- [blocked] policy-heuristic-eval: Implement a heuristic state-evaluation function at `src/vgc_ai/eval/heuristic.py` returning a single float for "side 0's expected advantage", based on: remaining HP totals, fainted count, simple type-advantage check using vgc2's type chart, and currently-active Pokemon's hp/maxhp ratio. Wire it into a new `HeuristicBattlePolicy` (registered as `heuristic` in `POLICIES`) that, for each action choice, simulates the action 1 ply forward and picks the action maximizing the heuristic. Acceptance: 200-game bench `heuristic` vs `greedy` shows `win_rate_a > 0.5` AND `ci95_low > 0.5` AND `avg_battle_ms < 5000`. If `avg_battle_ms` is over budget, mark blocked — do not weaken the heuristic just to fit budget. Why: implemented on branch `auto/policy-heuristic-eval-20260511`; ruff/pytest/import-acceptance pass; 200-game bench `heuristic` vs `greedy`: win_rate_a=0.545, **ci95_low=0.4758** (< 0.5 gate), ci95_high=0.6125, avg_battle_ms=197 (well under budget). Win rate is positive but the Wilson lower bound is below 0.5, so the edge over Greedy is not statistically distinguishable from a coin flip at n=200. One weight-tuning iteration (FAINT_WEIGHT 3→6, MATCHUP_WEIGHT 0.5→1.0) at n=100 returned 50-50, so naïve weight bumps don't help. Unblock by: (a) tightening the heuristic — try a damage-prediction term in eval, or use ZERO_RNG-deterministic rollouts in the forward sim to reduce stochastic noise; or (b) widening n to ~500+ if true win rate is genuinely ~0.55, to push ci95_low above 0.5.

## Proposed for human review

<!-- Claude appends here when it has an idea but is not allowed to act on it. You move them to Approved (or delete) when you triage. -->

<!-- Triage log (most-recent first):
     2026-05-14:
       - chore-gitignore-models → promoted to Approved [todo] (priority 1: PR-sanitation, 1-line fix)
       - chore-baseline-promote-heuristic-det → promoted to Approved [todo] (priority 2: lock in the new baseline before further policy work)
       - policy-tabular-mc-canonicalize-action-idx → promoted to Approved [todo] (priority 3: unblock MC family)
       - policy-heuristic-eval-det-2ply → promoted to Approved [todo] (priority 4: push heuristic family further)
     2026-05-13:
       - policy-heuristic-eval-deterministic-rollout-n2000 → promoted to Approved [todo]
       - policy-tabular-mc-train-with-loss-signal → promoted to Approved [todo]
       - policy-tabular-mc-train-vs-greedy-warmup → promoted to Approved [todo]
       - policy-heuristic-eval-deterministic-rollout-n500 → deleted (already in Approved as [blocked])
       - policy-tabular-mc-train → deleted (already in Approved as [blocked])
     2026-05-12:
       - bench-elo-gitignore, policy-greedy-switch-n500, policy-heuristic-eval-n500, policy-heuristic-eval-deterministic-rollout → promoted
       - policy-greedy-switch-tighten → split into 4 tasks (-a/-b/-c/-d) and promoted
       - mypy-competitor-subclass-fix → deleted (already fixed in 60f21be, closes #1)
-->

## Done

<!-- Most-recent first. Prune manually when this gets long. -->

- [done] policy-random-bench-baseline: `RandomBattlePolicy` was already exposed by vgc2 and registered in `vgc_ai.cli.POLICIES`. Bench round confirms Greedy ~90% vs Random in doubles. No code change needed. (Pre-loop, marked done at deploy.)
- [done] bench-leaderboard: Implemented in `bench/run_continuous.py` (B2 commit). Appends rows to `bench/leaderboard.csv` with the schema in the file's header.
- [done] bench-smoke: Implemented in `bench/run_once.py` (B2 commit). Prints structured JSON with `policy_a`, `policy_b`, `wins_a`, `wins_b`, `ties`, `win_rate_a`, Wilson `ci95_low`/`ci95_high`, `elapsed_sec`, `avg_battle_ms`. Smoke-tested on the VM (greedy vs random: 20-0-0 in 0.4s).

## Blocked

<!-- Tasks that hit an obstacle. Claude moves them here with a "Why: ..." line so you can unblock. -->
