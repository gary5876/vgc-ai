# vgc-ai TASKS

> **Work queue for the autonomous Claude loop running on the GCP VM.**
> The loop reads this file, picks the top **Approved** item with status `todo`, and works only on that.
> The loop NEVER invents tasks. If it has an idea, it appends to **Proposed for human review**.
>
> **You** (the human) maintain the Approved list. **Claude** updates statuses (`todo` → `wip` → `done` / `blocked`).
> Format: `- [STATUS] <slug>: <description>`.

## Approved

<!-- Pre-approved tasks Claude can pick from. Keep ordered by priority — Claude takes the top todo. -->

- [blocked] policy-registry: Extract `POLICIES` out of `src/vgc_ai/cli.py` into a new module `src/vgc_ai/policies/registry.py` so `bench/` and other consumers can import it without depending on the CLI module. Update `bench/run_once.py` and `bench/run_continuous.py` to import from the new location. Update `src/vgc_ai/cli.py` to re-export for backwards compatibility. Acceptance: `python -c "from vgc_ai.policies.registry import POLICIES; print(sorted(POLICIES))"` prints at least `['greedy', 'random', 'tree']`; `uv run pytest`, `uv run ruff check`, `uv run mypy --strict src` all pass; bench/run_once.py still works. No bench evidence required (this is a refactor, not a policy change). Why: implemented on branch `auto/policy-registry-20260511` but bench/import-acceptance pass — was blocked on issue #1 which is now fixed on main. **Human action needed:** either open a PR for that existing branch manually (it should rebase cleanly), or re-mark this `[todo]` so the loop redoes it on a fresh branch.

- [done] bench-fixed-teams: Add a `--fixed-team-seed N` flag to `bench/run_once.py` (and propagate through `bench/run_continuous.py` and `vgc_ai.eval.duel.duel`) so that, when set, the same `seed` is used to generate teams across all N battles in the round, making different policies compete on identical battles and dramatically reducing variance. Default behaviour (no flag) stays as today (fresh random teams per battle). Acceptance: `bench.run_once --a greedy --b random --n 5 --fixed-team-seed 42` returns the same `wins_a` count across 3 invocations; existing tests stay green; new test asserts determinism. No policy bench evidence required (infrastructure change). PR #3.

- [done] bench-elo: Maintain a running Elo rating per policy at `bench/elo.csv` (cols: `timestamp,policy,games,wins,losses,elo`). Each call to `run_continuous.py` updates Elo using the new matchup results (K=32, R0=1500). Acceptance: after 50 rounds of greedy-vs-random benching, `greedy`'s Elo is at least 200 above `random`'s. Add a unit test that simulates two rounds of fake results and asserts the Elo update formula matches a hand-computed value. **Verified:** 50 rounds × 20 battles → greedy 1711.17, random 1288.83, gap = 422.34. PR #4.

- [done] competitor-self-validate: Add `scripts/validate_competitor.py` that constructs two `VgcAiCompetitor()` instances and runs `vgc2.competition.match.Match` between them for 10 doubles battles (`n_active=2`), asserting that the match completes without error and reports a winner per battle. Used as a competition-submission smoke test. Acceptance: `uv run python scripts/validate_competitor.py` exits 0 in under 60 seconds; prints final win count. No bench evidence required. **Verified:** exits 0 in ~0.7s; 18 battles ran (Match tiebreaker pushed past the n=10 floor); printed `wins_a=8 wins_b=10`. PR #5.

- [done] bench-summary: Add `bench/summary.py` (module + CLI `python -m bench.summary --last N`) that reads `leaderboard.csv` and prints a Markdown table aggregating the last `N` rounds: rows are policies, columns are `vs greedy`, `vs random` (and any future baselines), cell is `win_rate (n=games)`. CLI default `--last 50`. Acceptance: script runs against the current leaderboard, prints valid Markdown, exits 0 on an empty leaderboard (prints "no data yet"). **Verified:** `uv run python -m bench.summary` against the current 52-row leaderboard prints a valid Markdown table (greedy 95.80% n=1000 vs random); `--path /tmp/nope.csv` prints `no data yet` and exits 0; 9 new unit tests cover aggregation/render/CLI edges.

- [blocked] policy-greedy-switch: Add a new battle policy `GreedyWithSwitchBattlePolicy` at `src/vgc_ai/policies/battle.py` that subclasses or composes `GreedyBattlePolicy` with one rule on top: if the active Pokemon's HP fraction is below 0.25 AND at least one benched Pokemon (a) is not fainted and (b) has a STAB move whose type is super-effective against the opponent's active, switch to that bench instead of attacking. Otherwise fall back to greedy. Register as `greedy_switch` in `POLICIES`. Acceptance: 200-game bench `greedy_switch vs greedy` shows `win_rate_a > 0.5` AND `ci95_low > 0.5` AND `avg_battle_ms < 5000`. If the heuristic isn't expressible via the vgc2 API exposed by `BattlePolicy`, mark `blocked` with a concrete reason — do NOT monkey-patch vgc2. Why: implemented on branch `auto/policy-greedy-switch-20260511`; ruff/mypy --strict/pytest all green (incl. new `tests/test_greedy_switch.py`); 200-game bench `greedy_switch` vs `greedy`: win_rate_a=0.535, **ci95_low=0.4659** (< 0.5 gate), ci95_high=0.6028, avg_battle_ms=23.48 (well under budget). Win rate is positive but the Wilson lower bound is below 0.5, so the +3.5% edge over Greedy is not statistically distinguishable from a coin flip at n=200 — same shape as the `policy-heuristic-eval` block. Unblock paths proposed below.

- [done] policy-tabular-mc-skeleton: Implement the skeleton of a tabular first-visit Monte Carlo battle policy at `src/vgc_ai/policies/tabular_mc.py`, following the structure of AurelianTactics 2024 (3rd place): collapsed integer state encoder (target ~11 dims), action-value table backed by a `dict[tuple, list[float]]`, `learn(trajectories)` method that does first-visit MC updates, JSON save/load. Register as `tabular_mc` in `POLICIES` (untrained instance acts uniformly at random when state unseen — this is by design). Acceptance: unit tests assert (a) the state encoder is deterministic across two encodings of the same `State`, (b) the encoded tuple has the same length across 20 random states; smoke-bench `tabular_mc vs random` runs end-to-end without crashing (no win required; the policy is untrained, table is empty); `avg_battle_ms < 5000`. **Out of scope:** training the policy. That's a follow-up task once this skeleton lands. **Verified:** 11-dim encoder; 7 unit tests cover encoder determinism, length consistency across 20 random states, untrained legality, save/load round-trip, first-visit MC update math; smoke bench `tabular_mc` vs `random` n=200: 96-104-0 (untrained ≈ random, as expected), avg_battle_ms=10.19, avg_turn_ms_a=0.056ms. PR #7.



- [done] bench-turn-timing: Add `avg_turn_ms_a` and `avg_turn_ms_b` columns to the bench output (both `run_once.py` JSON and `run_continuous.py` CSV). To get per-turn timing, instrument `vgc_ai/eval/duel.py` to wrap each policy's `get_action` call (or equivalent) with `time.perf_counter()` and accumulate. If vgc2's `BattlePolicy` interface doesn't expose a hookable method, mark `blocked` with a concrete reason — do not monkey-patch vgc2 internals. Acceptance: leaderboard.csv has the two new columns populated with non-zero values; existing rows are preserved (the writer should handle the schema migration via header-rewrite on first new round). No policy bench evidence required. PR #2.

- [blocked] policy-heuristic-eval: Implement a heuristic state-evaluation function at `src/vgc_ai/eval/heuristic.py` returning a single float for "side 0's expected advantage", based on: remaining HP totals, fainted count, simple type-advantage check using vgc2's type chart, and currently-active Pokemon's hp/maxhp ratio. Wire it into a new `HeuristicBattlePolicy` (registered as `heuristic` in `POLICIES`) that, for each action choice, simulates the action 1 ply forward and picks the action maximizing the heuristic. Acceptance: 200-game bench `heuristic` vs `greedy` shows `win_rate_a > 0.5` AND `ci95_low > 0.5` AND `avg_battle_ms < 5000`. If `avg_battle_ms` is over budget, mark blocked — do not weaken the heuristic just to fit budget. Why: implemented on branch `auto/policy-heuristic-eval-20260511`; ruff/pytest/import-acceptance pass; 200-game bench `heuristic` vs `greedy`: win_rate_a=0.545, **ci95_low=0.4758** (< 0.5 gate), ci95_high=0.6125, avg_battle_ms=197 (well under budget). Win rate is positive but the Wilson lower bound is below 0.5, so the edge over Greedy is not statistically distinguishable from a coin flip at n=200. One weight-tuning iteration (FAINT_WEIGHT 3→6, MATCHUP_WEIGHT 0.5→1.0) at n=100 returned 50-50, so naïve weight bumps don't help. Unblock by: (a) tightening the heuristic — try a damage-prediction term in eval, or use ZERO_RNG-deterministic rollouts in the forward sim to reduce stochastic noise; or (b) widening n to ~500+ if true win rate is genuinely ~0.55, to push ci95_low above 0.5.

## Proposed for human review

<!-- Claude appends here when it has an idea but is not allowed to act on it. You move them to Approved (or delete) when you triage. -->

- policy-heuristic-eval-deterministic-rollout: Unblock path (a) for `policy-heuristic-eval`. Modify the 1-ply forward simulation inside `HeuristicBattlePolicy` to use vgc2's `ZERO_RNG` (or equivalent deterministic damage/accuracy path) so the eval comparison across candidate actions isn't dominated by simulator stochasticity. Keep the heuristic weights as currently checked in on `auto/policy-heuristic-eval-20260511`. Acceptance: same gate as the original task (n=200, `win_rate_a > 0.5`, `ci95_low > 0.5`, `avg_battle_ms < 5000`). Rationale: weight-tuning at n=100 returned 50-50, so the issue is more likely sim noise than weight calibration.

- policy-heuristic-eval-n500: Unblock path (b) for `policy-heuristic-eval`. Re-run the existing `heuristic` vs `greedy` bench on the auto/policy-heuristic-eval-20260511 branch at n=500 to see if the Wilson lower bound clears 0.5 (point estimate was 0.545 at n=200, ci95_low=0.4758). Acceptance: `bench/results/policy-heuristic-eval.json` at n=500 satisfies the standard gate. Cheap: ~5× the prior runtime (200 games took seconds). Do this before (a) if (a) turns out to be invasive.

- policy-greedy-switch-n500: Unblock path for `policy-greedy-switch`. Re-run the existing `greedy_switch` vs `greedy` bench on the auto/policy-greedy-switch-20260511 branch at n=500 to see if the Wilson lower bound clears 0.5 (point estimate was 0.535 at n=200, ci95_low=0.4659). Acceptance: `bench/results/policy-greedy-switch.json` at n=500 satisfies the standard gate. Cheap: ~5× the prior 4.7s runtime (~25s total). Try this first.

- policy-greedy-switch-tighten: Unblock path for `policy-greedy-switch`. Tighten the switch heuristic to reduce noise from speculative switches that happen to land into a worse matchup. Concrete tweaks to try (one per branch): (a) require the bench mon to be at full HP before switching in; (b) limit to at most one switch per turn in doubles even if both actives are below the HP threshold; (c) score candidate reserves by max type-effectiveness * STAB-modifier instead of returning the first match; (d) require the candidate's super-effective opponent to be the same opponent currently in front of the low-HP active (slot-aligned), not "any opponent". Acceptance: same gate as the original task at n=200.

- mypy-competitor-subclass-fix: Address issue #1 — `src/vgc_ai/competitor.py:19` "Class cannot subclass 'Competitor' (has type 'Any')" — so `mypy --strict src` is green on `main`. This unblocks `policy-registry`. Likely fix: add a `type: ignore[misc]` on the class line with a comment pointing to vgc2 being untyped, OR add a minimal `Protocol`/stub for `vgc2.competition.Competitor` under a `stubs/` dir referenced from `pyproject.toml`. Acceptance: `uv run mypy --strict src` exits 0; existing `uv run pytest` stays green. No bench evidence required (typing-only).

- bench-elo-gitignore: Add `bench/elo.csv` to `.gitignore`. The file is a runtime artifact produced/updated by `bench/run_continuous.py` (from the completed bench-elo task) and is currently untracked but not ignored — every autonomous loop iteration sees it in `git status`. CLAUDE.md explicitly lists "ELO traces" alongside "Training logs, MC visit tables" as artifacts that must be gitignored. One-line change to `.gitignore` next to the existing `bench/leaderboard.csv` line; no code touched. Acceptance: `git status` is clean after a `run_continuous.py` round; existing `bench/leaderboard.csv` ignore behavior unchanged. No bench evidence required (housekeeping).

## Done

<!-- Most-recent first. Prune manually when this gets long. -->

- [done] policy-random-bench-baseline: `RandomBattlePolicy` was already exposed by vgc2 and registered in `vgc_ai.cli.POLICIES`. Bench round confirms Greedy ~90% vs Random in doubles. No code change needed. (Pre-loop, marked done at deploy.)
- [done] bench-leaderboard: Implemented in `bench/run_continuous.py` (B2 commit). Appends rows to `bench/leaderboard.csv` with the schema in the file's header.
- [done] bench-smoke: Implemented in `bench/run_once.py` (B2 commit). Prints structured JSON with `policy_a`, `policy_b`, `wins_a`, `wins_b`, `ties`, `win_rate_a`, Wilson `ci95_low`/`ci95_high`, `elapsed_sec`, `avg_battle_ms`. Smoke-tested on the VM (greedy vs random: 20-0-0 in 0.4s).

## Blocked

<!-- Tasks that hit an obstacle. Claude moves them here with a "Why: ..." line so you can unblock. -->
